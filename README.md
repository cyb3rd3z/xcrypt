PROJECT XCRYPT:

    simple file encryption terminal program that encrypts data in file and decrypts it after given key.

    Details:

        - rust 1.85.1
        - we use wrapping add for encryption
        - we use wrapping sub for decryption

    future implementations:

        - gui
        - AES algo
